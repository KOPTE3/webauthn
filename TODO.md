1. Нужна сущность типа `state` для состояний.
Например,`Page#{auth, refresh, reload, isActiveUser}` все это нужно вынести из объекта страницы

2. Требуется покрытие тестами

3. `--grep` должен работать и без `suite`
